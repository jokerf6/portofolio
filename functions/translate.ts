// import fr from "../public/locales/fr.json";
// import en from "../public/locales/en.json";

// export default function translate(title: string, language: any) {
//   console.log(language);
//   if (language === "fr") {
//     return (fr as { [key: string]: string })[title] || title;
//   } else {
//     return (en as { [key: string]: string })[title] || title;
//   }
// }
